spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paw_db
    username: Need to change
    password: Need to change
    driver-class-name: org.postgresql.Driver
  security:
      oauth2:
        client:
          registration:
            kakao:
              client-id: Need to change
  custom:
    jwt:
      secretKey: Need to change
    accessToken:
      expirationSeconds: "#{60*15}"